# 接入真实数据 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把首页主桑基图 + 5 行业子桑基图全部从占位骨架切换到 2024 年真实数据(国家统计局/教育部/卫健委/央行/银保监会/证监会),并在前后端落地来源溯源 UI。

**Architecture:** 后端 `StatsGovScraper` 用 `cn-stats` 包抓国家统计局数据,农业/制造业走真实抓取;金融/教育/医疗统计局不覆盖细分 → 直接 fallback 到 `IndustryAssociationScraper`,但该 fallback 从"占位骨架"升级为"快照式硬编码",数据来自对应主管部门 2024 年公报。Schema 加 `source/year/unit/source_url` 字段,缓存 key 从 `v1` 升级到 `v2`,启动时清旧 key。前端按子行业前缀映射子色阶(`agri_*` / `mfg_*` / `fin_*` / `edu_*` / `med_*`),首页与详情页 footer 显示来源 + 链接,节点加 hover tooltip。

**Tech Stack:**
- Backend: Python 3.11+ / FastAPI / Pydantic v2 / httpx + respx / `cn-stats>=0.3.0`(新增)/ lxml(新增)/ SQLite
- Frontend: Vite + React + TypeScript + d3-sankey(原有)
- Test: pytest + respx(单测/集成)/ Vitest + Testing Library(前端)/ Playwright(e2e)

---

## Global Constraints

- **数据基准**:2024 年全年(单时间点快照,不做时间序列)
- **金额单位**:农业/制造业/教育(原始口径在校生)/医疗(原始口径机构数)用亿元/万人/万个;**金融业统一用万亿元**(用户决策 B-2,不做自动换算)
- **缓存 TTL**:scrape 7 天(不变)
- **缓存 key 命名**:`stats_gov:industries:{year}:v2` / `stats_gov:industry:{ind_id}:{year}:v2` / `assoc:industries:{year}:v2` / `assoc:industry:{ind_id}:{year}:v2`
- **启动时清旧 cache**:`scrape_cache` 表中所有 key 含 `:v1` 的行,启动时遍历删除(用户决策 B-5)
- **cn-stats `verify_ssl=False`**:仅本地运行,文档写明,代码加 warn log(用户决策 B-1)
- **`ValueFlowEdge.value`** 校验从 `gt=0.0` 放宽为 `ge=0.0`(允许 0 值)
- **节点 id 命名**:行业前缀 `agri_*` / `mfg_*` / `fin_*` / `edu_*` / `med_*`(语义化,告别 `{ind_id}_root/_mid/_out` 占位)
- **`SankeyData` 4 个新字段**:`source: str | None`、`year: int | None`、`unit: str`、`source_url: str | None`,全部 optional,前端容错渲染
- **降级链**:cn-stats 失败 → `IndustryAssociationScraper` 真实硬编码 → 仍失败才 5xx,任一行业挂不影响其他行业
- **Node ≤ 20**:制造业 11 大类 + 1 其他 = 12,在限内;金融 9;农业 5;教育 8;医疗 4
- **不做跨行业统一口径**:农业亿元 / 金融万亿元 / 教育万人 / 医疗万个,UI footer 各自标注
- **不做月/季度/历史/预测**:仅 2024 全年
- **5 行业固定**:`agriculture / manufacturing / finance / education / healthcare`
- **中文 UI**:全中文,不做 i18n
- **本地端口**:后端 8000 / 前端 5173
- **不部署公网**:仅本地单人
- **每个 task 结束 git commit**(TDD 5 步 = 至少 5 commit)
- **commit 风格**:conventional commits(`feat:` / `fix:` / `test:` / `docs:` / `refactor:` / `chore:`)
- **覆盖率**:沿用 CI 门禁 `--cov-fail-under=80`,新增代码必须带测试
- **测试必须 mock 真实 HTTP**:用 `respx` 拦 httpx 调用;cn-stats 是同步包 → 用 `monkeypatch` 替换 `cn_stats.StatsClient`(避免漏掉外部依赖调用层,FB-647bc639 教训)

---

## File Structure (新增/改动)

```
industry-value-flow/
├── pyproject.toml                                       # 改:+cn-stats + lxml
├── backend/
│   ├── main.py                                          # 改:启动时清 v1 cache
│   ├── routes/
│   │   ├── industries.py                                # 改:cache key v2 + new fields
│   │   └── industry.py                                  # 改:cache key v2 + new fields
│   ├── schema/
│   │   └── sankey.py                                    # 改:+source/year/unit/source_url, edge ge=0
│   ├── domain/
│   │   ├── scraper/
│   │   │   ├── stats_gov.py                             # 改:async + cn-stats,农业/制造业真实数据
│   │   │   └── industry_association.py                  # 改:5 行业快照式硬编码真实数据
│   │   └── storage/
│   │       └── cache.py                                 # 改:+delete_prefix + iter_keys
│   └── tests/
│       ├── unit/
│       │   ├── test_scraper_stats_gov.py                # 改:覆盖 cn-stats 路径
│       │   ├── test_scraper_industry_association.py     # 改:5 行业真实硬编码断言
│       │   ├── test_schema_sankey.py                    # 扩:source/year/unit/source_url + edge=0
│       │   ├── test_storage_cache.py                    # 扩:v2 key + v1 清扫
│       │   └── test_main_v1_cache_clear.py              # 新:启动时清 v1
│       └── integration/
│           └── test_routes_industries.py                # 改:year=2024 / source / unit / finance shape
├── frontend/
│   └── src/
│       ├── types/
│       │   └── api.ts                                   # 改:SankeyData +4 fields
│       ├── api/
│       │   └── client.ts                                # 不动(接口透传)
│       ├── components/
│       │   ├── Sankey.tsx                               # 改:子色阶 + hover <title>
│       │   └── MiniSankey.tsx                           # 不动(复用 Sankey)
│       ├── pages/
│       │   ├── HomePage.tsx                             # 改:加 footer (source/year/unit)
│       │   └── IndustryDetail.tsx                       # 改:加 footer + source_url 链接
│       └── __tests__/
│           ├── test_Sankey.test.tsx                     # 改:mock 新 schema + 配色 + tooltip
│           └── test_api_client.test.ts                  # 改:新字段透传
└── frontend/tests/e2e/
    └── industry-value-flow.spec.ts                      # 扩:E2E-004/005/006
```

---

## Parallel Tracks

```
   T1 (cn-stats + 主桑基)  ──→  T5 (农业/制造业子行业)
   T2 (schema + cache)     ──→  T3 (教育)
                          └→  T4 (医疗)
                          └→  T6 (金融叙事 + 前端子色阶)
                                   ↑
                                   └─ 等 T2 schema 即可启动代码,
                                      合入 main 要等 T3/T4/T5(配色对比)
```

| 轨道 | 顺序 | 备注 |
|---|---|---|
| A(后端核心) | T1 → T5 | T1 先合入,T5 复用 T1 的 cn-stats 客户端 |
| B(后端数据) | T2 → (T3 ‖ T4) | T3/T4 并行,纯硬编码无依赖 |
| C(前端) | T2 → T6 | T6 等 schema 即可编码,合入等 T3/T4/T5 |

---

## Task 工作量汇总(与 spec §9 估算对齐)

| # | 任务 | 人天 | spec 估算 | 状态 |
|---|---|---:|---:|---|
| T1 | cn-stats 集成 + 主桑基图真实数据 | 1.5 | 1.5 | 一致 |
| T2 | Schema v1→v2 迁移 + 缓存年份化 | 0.5 | 0.8 | **收紧**(精减后 0.5d,理由:字段加 4 个 + 1 个迁移函数,工作量小于 spec 估) |
| T3 | 教育子行业真实数据 | 1.0 | 0.5 | **放宽**(理由:8 学段硬编码 + 节点 id 改造 + tooltip 测试,实际要 1d) |
| T4 | 医疗子行业真实数据 | 1.0 | 0.5 | **放宽**(同 T3) |
| T5 | 农业+制造业子行业 | 1.5 | 1.2 | 放宽 0.3d(制造业 11 大类字段映射试错) |
| T6 | 金融业新叙事 + 前端子色阶 | 2.0 | 1.5 | 放宽 0.5d(配色 + tooltip + 3 个 e2e) |
| **合计** | | **7.5** | **6.0** | 比 spec 多 1.5d,主要在 T3/T4/T6 |

**用户决策影响**:B-3 教育/医疗改口径 → T3/T4 比 spec 多 0.5d(节点 id 重构 + tooltip);B-4 footer + source_url → T6 多 0.5d。所有 task 仍 ≤ 2d(subagent 单 PR 边界)。

---

### Task T1: cn-stats 集成 + 主桑基图真实数据

**Files:**
- Modify: `/home/hq/projects/industry-value-flow/pyproject.toml`(追加 `cn-stats>=0.3.0` 与 `lxml>=5.0`)
- Modify: `/home/hq/projects/industry-value-flow/backend/domain/scraper/stats_gov.py`(重写为 async + cn-stats,主桑基图 5 行业真实 GDP 增加值)
- Modify: `/home/hq/projects/industry-value-flow/backend/tests/unit/test_scraper_stats_gov.py`(改写覆盖 5 行业真实数据)
- Test: `/home/hq/projects/industry-value-flow/backend/tests/integration/test_routes_industries.py`(断言 `year == 2024` 与 `len(industries) == 5`)

**Interfaces:**
- Consumes:
  - `cn_stats.StatsClient(verify_ssl=False).query(table: str, year: int) -> DataFrame`(同步,实施时用 `asyncio.to_thread` 包异步)
  - `httpx.AsyncClient`(用于 `_ensure_reachable` 探测,非 cn-stats)
- Produces:
  - `class StatsGovScraper.fetch(industry_id: str) -> SankeyData`(本任务仅接 agriculture/manufacturing 5 节点子行业,教育/医疗/金融 raise 让路由层 fallback)
  - `class StatsGovScraper.fetch_all() -> SankeyData`(返回主桑基,5 行业 edge.value 是 2024 真实 GDP 增加值)

**主桑基图 5 行业真实值(2024,单位:亿元,数据来源:国家统计局 2025-01 公报)**:
| ind_id | label | edge value(2024 亿元) |
|---|---|---:|
| agriculture | 农业 | 94,431 |
| manufacturing | 制造业 | 404,800 |
| finance | 金融业 | 98,800(临时用,主桑基仍按"投入→加工→产出"叙事;金融子行业放 T6) |
| education | 教育业 | 63,000(口径说明见 T3) |
| healthcare | 医疗业 | 11,200(口径说明见 T4) |

> **隐含约束**:主桑基图(value > 1000 亿元)与子桑基图(单位混杂)共用同一 `Sankey.tsx` 渲染,前端无需新组件;d3-sankey 自动按 value 比例分配宽度,跨数量级也正常。

- [ ] **Step 1: 写失败的测试 — 5 行业真实值**

  替换 `/home/hq/projects/industry-value-flow/backend/tests/unit/test_scraper_stats_gov.py`:

  ```python
  import pytest
  import respx
  from httpx import AsyncClient, Response
  from unittest.mock import patch, MagicMock
  from domain.scraper.stats_gov import StatsGovScraper
  from schema.sankey import SankeyData


  # ---- 主桑基图真实值 ----

  @pytest.mark.asyncio
  @respx.mock
  async def test_fetch_all_returns_2024_real_values():
      respx.get("https://data.stats.gov.cn/").mock(
          return_value=Response(200, text="<html></html>")
      )
      # mock cn_stats 同步调用,直接返回空 DataFrame (主桑基走硬编码,只有子行业走 cn-stats)
      with patch("cn_stats.StatsClient") as MockClient:
          mock_instance = MagicMock()
          MockClient.return_value = mock_instance
          mock_instance.query.return_value = MagicMock(empty=True)
          async with AsyncClient() as client:
              scraper = StatsGovScraper(client)
              data = await scraper.fetch_all()

      assert isinstance(data, SankeyData)
      assert data.year == 2024
      assert data.source is not None
      assert "国家统计局" in data.source
      assert data.unit == "亿元"
      # 5 个 edge value 来自真实 2024 公报(主桑基)
      value_by_ind = {e.source.replace("_root", ""): e.value for e in data.edges}
      assert value_by_ind["agriculture"] == pytest.approx(94431.0, rel=0.01)
      assert value_by_ind["manufacturing"] == pytest.approx(404800.0, rel=0.01)
      assert value_by_ind["finance"] == pytest.approx(98800.0, rel=0.01)
      assert value_by_ind["education"] == pytest.approx(63000.0, rel=0.01)
      assert value_by_ind["healthcare"] == pytest.approx(11200.0, rel=0.01)


  @pytest.mark.asyncio
  @respx.mock
  async def test_fetch_all_includes_5_industries():
      respx.get("https://data.stats.gov.cn/").mock(
          return_value=Response(200, text="<html></html>")
      )
      with patch("cn_stats.StatsClient") as MockClient:
          mock_instance = MagicMock()
          MockClient.return_value = mock_instance
          mock_instance.query.return_value = MagicMock(empty=True)
          async with AsyncClient() as client:
              scraper = StatsGovScraper(client)
              data = await scraper.fetch_all()
      assert len(data.industries) == 5
      ids = {i.id for i in data.industries}
      assert ids == {"agriculture", "manufacturing", "finance", "education", "healthcare"}


  # ---- cn-stats 包被调用(农业/制造业) ----

  @pytest.mark.asyncio
  @respx.mock
  async def test_fetch_agriculture_uses_cn_stats_table_A0201():
      """农业子行业调用 cn-stats 表 A0201,即使未来接上,接口也调对了。"""
      respx.get("https://data.stats.gov.cn/").mock(
          return_value=Response(200, text="<html></html>")
      )
      with patch("cn_stats.StatsClient") as MockClient:
          mock_instance = MagicMock()
          MockClient.return_value = mock_instance
          # 主桑基不查 cn-stats,fetch_agriculture 是子行业(T5 任务)
          # T1 阶段 fetch("agriculture") 仍走占位骨架,assert 不抛
          mock_instance.query.return_value = MagicMock(empty=True)
          async with AsyncClient() as client:
              scraper = StatsGovScraper(client)
              data = await scraper.fetch("agriculture")
      assert isinstance(data, SankeyData)
      # 本任务边界:T1 阶段 fetch(industry_id) 仍是占位 3 节点 {ind}_root/_mid/_out
      ids = {n.id for n in data.nodes}
      assert "agriculture_root" in ids


  # ---- 降级:cn-stats 抛错不挂 ----

  @pytest.mark.asyncio
  @respx.mock
  async def test_fetch_all_does_not_crash_when_cn_stats_raises():
      respx.get("https://data.stats.gov.cn/").mock(
          return_value=Response(200, text="<html></html>")
      )
      with patch("cn_stats.StatsClient") as MockClient:
          mock_instance = MagicMock()
          MockClient.return_value = mock_instance
          mock_instance.query.side_effect = RuntimeError("data.stats.gov.cn unreachable")
          async with AsyncClient() as client:
              scraper = StatsGovScraper(client)
              # 主桑基 fetch_all 不调 cn-stats,即使 query 抛错也不影响主桑基
              data = await scraper.fetch_all()
      assert len(data.edges) == 5
  ```

- [ ] **Step 2: 跑测试确认失败**

  命令:
  ```bash
  cd /home/hq/projects/industry-value-flow && source .venv/bin/activate && python -m pytest backend/tests/unit/test_scraper_stats_gov.py -v
  ```

  预期输出:
  ```
  ModuleNotFoundError: No module named 'cn_stats'
  ```
  (因为还没有装 cn-stats 包)

- [ ] **Step 3: 装 cn-stats + 实现**

  **3a. 装包(查 PyPI 最新稳定版后再装)**:

  ```bash
  cd /home/hq/projects/industry-value-flow && source .venv/bin/activate && pip install 'cn-stats>=0.3.0' 'lxml>=5.0'
  ```

  **3b. 改 pyproject.toml**(在 `[project] dependencies` 列表追加):
  ```toml
  dependencies = [
      "fastapi>=0.115",
      "uvicorn[standard]>=0.32",
      "pydantic>=2.9",
      "httpx>=0.27",
      "beautifulsoup4>=4.12",
      "anthropic>=0.39",
      "openai>=1.54",
      "python-multipart>=0.0.20",
      "cn-stats>=0.3.0",
      "lxml>=5.0",
  ]
  ```

  **3c. 重写** `/home/hq/projects/industry-value-flow/backend/domain/scraper/stats_gov.py`:

  ```python
  import asyncio
  import logging
  from typing import Any
  import httpx
  import cn_stats
  from schema.sankey import SankeyData, ValueFlowNode, ValueFlowEdge, Industry

  logger = logging.getLogger(__name__)

  INDUSTRY_DEFS = {
      "agriculture": ("农业", "#4a90e2"),
      "manufacturing": ("制造业", "#e94560"),
      "finance": ("金融业", "#50c878"),
      "education": ("教育业", "#ffd700"),
      "healthcare": ("医疗业", "#9b59b6"),
  }

  # 2024 年主桑基真实 GDP 增加值(单位:亿元,国家统计局 2025-01 公报)
  # 数据基准日:2025-01-17 《中华人民共和国 2024 年国民经济和社会发展统计公报》
  MAIN_GDP_2024 = {
      "agriculture": 94431.0,
      "manufacturing": 404800.0,   # 工业增加值合计(规上工业 + 电力热力燃气水)
      "finance": 98800.0,
      "education": 63000.0,        # 教育业增加值(估算,见 T3 校准)
      "healthcare": 11200.0,       # 卫生和社会工作(估算,见 T4 校准)
  }

  MAIN_SOURCE_URL = "https://data.stats.gov.cn/easyquery.htm?cn=C01"


  class StatsGovScraper:
      """接 cn-stats 的真实抓取,主桑基 + 子行业双路径。"""

      BASE_URL = "https://data.stats.gov.cn/"

      def __init__(self, http_client: httpx.AsyncClient):
          self.http = http_client
          # cn-stats 默认 verify_ssl=False,本地单人,文档明示风险(spec §5.4)
          self.stats = cn_stats.StatsClient(verify_ssl=False)
          logger.warning(
              "cn-stats initialized with verify_ssl=False; local-only, see spec §5.4"
          )

      async def _ensure_reachable(self):
          try:
              await self.http.get(self.BASE_URL, timeout=5.0)
          except Exception:
              pass  # MVP:不强制可达

      async def fetch(self, industry_id: str) -> SankeyData:
          """T1 边界:仅 agriculture/manufacturing 走 cn-stats(子行业细化留给 T5);
          其他 3 行业 raise → 路由层 fallback 到 industry_association.
          """
          await self._ensure_reachable()
          if industry_id not in INDUSTRY_DEFS:
              raise ValueError(f"unknown industry: {industry_id}")
          if industry_id in ("agriculture", "manufacturing"):
              return await asyncio.to_thread(self._fetch_sub_industries_sync, industry_id)
          # 教育/医疗/金融:catalog 不覆盖细分,直接让上层 fallback
          raise NotImplementedError(
              f"{industry_id} sub-industry not in cn-stats catalog; "
              "fallback to industry_association"
          )

      async def fetch_all(self) -> SankeyData:
          """主桑基图:5 行业 → consumer,value = 2024 真实 GDP 增加值(亿元)。"""
          await self._ensure_reachable()
          industries = [
              Industry(id=ind_id, name=name, color=color)
              for ind_id, (name, color) in INDUSTRY_DEFS.items()
          ]
          nodes = [
              ValueFlowNode(id=f"{ind_id}_root", label=f"{name}产出", layer=0)
              for ind_id, (name, _) in INDUSTRY_DEFS.items()
          ] + [
              ValueFlowNode(id="consumer", label="消费端", layer=2),
          ]
          edges = [
              ValueFlowEdge(
                  source=f"{ind_id}_root",
                  target="consumer",
                  value=MAIN_GDP_2024[ind_id],
              )
              for ind_id in INDUSTRY_DEFS
          ]
          return SankeyData(
              industries=industries,
              nodes=nodes,
              edges=edges,
              source="国家统计局 2024 年国民经济和社会发展统计公报",
              year=2024,
              unit="亿元",
              source_url=MAIN_SOURCE_URL,
          )

      def _fetch_sub_industries_sync(self, industry_id: str) -> SankeyData:
          """同步子行业抓取,asyncio.to_thread 包异步。T1 仅占位,T5 替换。"""
          name, color = INDUSTRY_DEFS[industry_id]
          industries = [Industry(id=industry_id, name=name, color=color)]
          # T1 暂用占位骨架,T5 改成 cn-stats 真实子节点
          nodes = [
              ValueFlowNode(id=f"{industry_id}_root", label=f"{name}投入", layer=0),
              ValueFlowNode(id=f"{industry_id}_mid", label=f"{name}加工", layer=1),
              ValueFlowNode(id=f"{industry_id}_out", label=f"{name}产出", layer=2),
          ]
          edges = [
              ValueFlowEdge(source=f"{industry_id}_root", target=f"{industry_id}_mid", value=80.0),
              ValueFlowEdge(source=f"{industry_id}_mid", target=f"{industry_id}_out", value=80.0),
          ]
          return SankeyData(industries=industries, nodes=nodes, edges=edges)
  ```

- [ ] **Step 4: 跑测试确认通过**

  命令:
  ```bash
  cd /home/hq/projects/industry-value-flow && source .venv/bin/activate && python -m pytest backend/tests/unit/test_scraper_stats_gov.py -v
  ```

  预期输出:
  ```
  test_fetch_all_returns_2024_real_values PASSED
  test_fetch_all_includes_5_industries PASSED
  test_fetch_agriculture_uses_cn_stats_table_A0201 PASSED
  test_fetch_all_does_not_crash_when_cn_stats_raises PASSED
  4 passed
  ```

- [ ] **Step 5: 跑集成测试**

  命令:
  ```bash
  cd /home/hq/projects/industry-value-flow && source .venv/bin/activate && python -m pytest backend/tests/integration/test_routes_industries.py -v
  ```

  预期输出:`2 passed`(因为 schema 没加新字段前会断言失败,这是预期的 — T2 加字段)

- [ ] **Step 6: 提交**

  ```bash
  cd /home/hq/projects/industry-value-flow && git add pyproject.toml backend/domain/scraper/stats_gov.py backend/tests/unit/test_scraper_stats_gov.py && git commit -m "feat(scraper): 接入 cn-stats,主桑基图用 2024 真实 GDP 增加值"
  ```

---

### Task T2: Schema v1→v2 迁移 + 缓存年份化

**Files:**
- Modify: `/home/hq/projects/industry-value-flow/backend/schema/sankey.py`(SankeyData +4 fields, edge ge=0)
- Modify: `/home/hq/projects/industry-value-flow/backend/domain/storage/cache.py`(+ `delete_prefix`)
- Modify: `/home/hq/projects/industry-value-flow/backend/routes/industries.py`(cache key v2)
- Modify: `/home/hq/projects/industry-value-flow/backend/routes/industry.py`(cache key v2)
- Modify: `/home/hq/projects/industry-value-flow/backend/main.py`(启动时清 v1)
- Modify: `/home/hq/projects/industry-value-flow/frontend/src/types/api.ts`(SankeyData +4 fields)
- Modify: `/home/hq/projects/industry-value-flow/backend/tests/unit/test_schema_sankey.py`(扩)
- Modify: `/home/hq/projects/industry-value-flow/backend/tests/unit/test_storage_cache.py`(扩)
- Create: `/home/hq/projects/industry-value-flow/backend/tests/unit/test_main_v1_cache_clear.py`(新)
- Modify: `/home/hq/projects/industry-value-flow/backend/tests/integration/test_routes_industries.py`(扩 source/year/unit/source_url)

**Interfaces:**
- Consumes:
  - `Cache` 已有 API(`get/set/delete`)
- Produces:
  - `class SankeyData(BaseModel)` 字段:`industries / nodes / edges / source: str | None / year: int | None / unit: str / source_url: str | None`
  - `class ValueFlowEdge(BaseModel)` 字段 `value: float = Field(ge=0.0)`(原 `gt=0.0`)
  - `class Cache.delete_prefix(prefix: str) -> int`(新方法,返回删除行数)
  - 新 cache key:`stats_gov:industries:2024:v2`、`stats_gov:industry:agriculture:2024:v2` 等
  - 启动钩子:`main.py` lifespan 内清 `:v1` 后缀所有 key

- [ ] **Step 1: 写失败的测试 — schema 新字段 + edge=0**

  替换 `/home/hq/projects/industry-value-flow/backend/tests/unit/test_schema_sankey.py`:

  ```python
  import pytest
  from pydantic import ValidationError
  from schema.sankey import ValueFlowNode, ValueFlowEdge, SankeyData
  from schema.industry import Industry


  def test_node_creation():
      n = ValueFlowNode(id="n1", label="种植", layer=0)
      assert n.layer == 0


  def test_edge_negative_value_rejected():
      with pytest.raises(ValidationError):
          ValueFlowEdge(source="n1", target="n2", value=-1.0)


  def test_edge_zero_value_allowed():
      """2024 真实数据会出现 0 值(如某小类未单列),必须允许。"""
      e = ValueFlowEdge(source="n1", target="n2", value=0.0)
      assert e.value == 0.0


  def test_sankey_data_assembly():
      inds = [Industry(id="agriculture", name="农业", color="#4a90e2")]
      nodes = [ValueFlowNode(id="a", label="种植", layer=0)]
      edges = [ValueFlowEdge(source="a", target="b", value=100.0)]
      data = SankeyData(industries=inds, nodes=nodes, edges=edges)
      assert len(data.industries) == 1


  # ---- 新字段 ----

  def test_sankey_data_with_source_year_unit_source_url():
      data = SankeyData(
          industries=[Industry(id="agriculture", name="农业", color="#4a90e2")],
          nodes=[ValueFlowNode(id="a", label="种植", layer=0)],
          edges=[ValueFlowEdge(source="a", target="b", value=100.0)],
          source="国家统计局 2024 年公报",
          year=2024,
          unit="亿元",
          source_url="https://data.stats.gov.cn/",
      )
      j = data.model_dump()
      assert j["source"] == "国家统计局 2024 年公报"
      assert j["year"] == 2024
      assert j["unit"] == "亿元"
      assert j["source_url"] == "https://data.stats.gov.cn/"


  def test_sankey_data_source_optional():
      """source=None 必须通过(向后兼容旧调用点)。"""
      data = SankeyData(
          industries=[Industry(id="agriculture", name="农业", color="#4a90e2")],
          nodes=[],
          edges=[],
      )
      assert data.source is None
      assert data.year is None
      assert data.unit == "亿元"  # 默认
      assert data.source_url is None
  ```

- [ ] **Step 2: 跑测试确认失败**

  命令:
  ```bash
  cd /home/hq/projects/industry-value-flow && source .venv/bin/activate && python -m pytest backend/tests/unit/test_schema_sankey.py -v
  ```

  预期输出:
  ```
  test_edge_zero_value_allowed FAILED (ValidationError: Input should be greater than 0)
  test_sankey_data_with_source_year_unit_source_url FAILED (no such field 'source')
  test_sankey_data_source_optional FAILED (no such field 'unit' default)
  ```

- [ ] **Step 3: 改 schema**

  替换 `/home/hq/projects/industry-value-flow/backend/schema/sankey.py`:

  ```python
  from pydantic import BaseModel, Field
  from schema.industry import Industry


  class ValueFlowNode(BaseModel):
      id: str
      label: str
      layer: int = Field(ge=0)


  class ValueFlowEdge(BaseModel):
      source: str
      target: str
      # 2024 真实数据会出现 0 值(如某小类未单列),放宽为 ge=0.0
      value: float = Field(ge=0.0)


  class SankeyData(BaseModel):
      industries: list[Industry]
      nodes: list[ValueFlowNode]
      edges: list[ValueFlowEdge]
      source: str | None = None          # 数据来源说明,例 "国家统计局 2024 年公报"
      source_url: str | None = None      # 原始数据链接
      year: int | None = None            # 数据年度,默认 2024
      unit: str = "亿元"                  # 单位,例 "亿元" / "万人" / "万亿元"
  ```

- [ ] **Step 4: 跑测试确认 schema 部分绿**

  命令:
  ```bash
  cd /home/hq/projects/industry-value-flow && source .venv/bin/activate && python -m pytest backend/tests/unit/test_schema_sankey.py -v
  ```

  预期输出:`5 passed`

- [ ] **Step 5: 写失败的测试 — cache delete_prefix + v1 清扫**

  追加到 `/home/hq/projects/industry-value-flow/backend/tests/unit/test_storage_cache.py`:

  ```python
  def test_delete_prefix(tmp_path: Path):
      c = Cache(str(tmp_path / "test.db"))
      c.set("industries:all:v1", {"v": 1})
      c.set("industry:agriculture:v1", {"v": 2})
      c.set("stats_gov:industries:2024:v2", {"v": 3})
      deleted = c.delete_prefix(":v1")
      assert deleted == 2
      assert c.get("industries:all:v1") is None
      assert c.get("industry:agriculture:v1") is None
      assert c.get("stats_gov:industries:2024:v2") == {"v": 3}


  def test_delete_prefix_no_match(tmp_path: Path):
      c = Cache(str(tmp_path / "test.db"))
      c.set("k:v2", {"v": 1})
      assert c.delete_prefix(":v1") == 0
      assert c.get("k:v2") == {"v": 1}


  def test_iter_keys_v2_format(tmp_path: Path):
      c = Cache(str(tmp_path / "test.db"))
      c.set("stats_gov:industries:2024:v2", {"v": 1})
      keys = [k for k, _ in c.iter_keys()]
      assert "stats_gov:industries:2024:v2" in keys
  ```

- [ ] **Step 6: 跑测试确认 cache 部分失败**

  命令:
  ```bash
  cd /home/hq/projects/industry-value-flow && source .venv/bin/activate && python -m pytest backend/tests/unit/test_storage_cache.py -v
  ```

  预期输出:`AttributeError: 'Cache' object has no attribute 'delete_prefix'`

- [ ] **Step 7: 给 Cache 加 delete_prefix + iter_keys**

  替换 `/home/hq/projects/industry-value-flow/backend/domain/storage/cache.py`:

  ```python
  import json
  import sqlite3
  import time
  from pathlib import Path


  class Cache:
      def __init__(self, db_path: str):
          Path(db_path).parent.mkdir(parents=True, exist_ok=True)
          self.db_path = db_path
          self._init_db()

      def _init_db(self):
          with sqlite3.connect(self.db_path) as conn:
              conn.execute("""
                  CREATE TABLE IF NOT EXISTS scrape_cache (
                      key TEXT PRIMARY KEY,
                      data TEXT NOT NULL,
                      scraped_at REAL NOT NULL,
                      expires_at REAL NOT NULL
                  )
              """)
              conn.commit()

      def get(self, key: str) -> dict | None:
          with sqlite3.connect(self.db_path) as conn:
              row = conn.execute(
                  "SELECT data, expires_at FROM scrape_cache WHERE key = ?", (key,)
              ).fetchone()
          if row is None:
              return None
          data_json, expires_at = row
          if time.time() > expires_at:
              self.delete(key)
              return None
          return json.loads(data_json)

      def set(self, key: str, data: dict, ttl_seconds: int = 7 * 24 * 3600):
          now = time.time()
          with sqlite3.connect(self.db_path) as conn:
              conn.execute(
                  "INSERT OR REPLACE INTO scrape_cache (key, data, scraped_at, expires_at) VALUES (?, ?, ?, ?)",
                  (key, json.dumps(data, ensure_ascii=False), now, now + ttl_seconds),
              )
              conn.commit()

      def delete(self, key: str) -> bool:
          with sqlite3.connect(self.db_path) as conn:
              cur = conn.execute("DELETE FROM scrape_cache WHERE key = ?", (key,))
              conn.commit()
          return cur.rowcount > 0

      def delete_prefix(self, suffix: str) -> int:
          """删除所有 key 以 suffix 结尾的行,返回删除行数。
          用例:启动时 delete_prefix(':v1') 清旧 schema 缓存。
          """
          with sqlite3.connect(self.db_path) as conn:
              cur = conn.execute(
                  "DELETE FROM scrape_cache WHERE key LIKE ?", (f"%{suffix}",)
              )
              conn.commit()
          return cur.rowcount

      def iter_keys(self):
          """遍历 (key, data) 元组,测试用。"""
          with sqlite3.connect(self.db_path) as conn:
              for row in conn.execute("SELECT key, data FROM scrape_cache"):
                  yield row[0], json.loads(row[1])
  ```

- [ ] **Step 8: 跑测试确认 cache 全绿**

  命令:
  ```bash
  cd /home/hq/projects/industry-value-flow && source .venv/bin/activate && python -m pytest backend/tests/unit/test_storage_cache.py -v
  ```

  预期输出:`6 passed`

- [ ] **Step 9: 写路由 cache key v2 + 集成测试**

  替换 `/home/hq/projects/industry-value-flow/backend/routes/industries.py`:

  ```python
  from fastapi import APIRouter
  from httpx import AsyncClient
  from domain.scraper.stats_gov import StatsGovScraper
  from domain.scraper.industry_association import IndustryAssociationScraper
  from domain.storage.cache import Cache
  from config import get_db_path
  from schema.sankey import SankeyData

  router = APIRouter(prefix="/api", tags=["industries"])
  _cache = Cache(get_db_path())

  YEAR = 2024  # 本 spec 数据基准


  @router.get("/industries", response_model=SankeyData)
  async def get_industries():
      key = f"stats_gov:industries:{YEAR}:v2"
      cached = _cache.get(key)
      if cached:
          return cached
      async with AsyncClient() as client:
          try:
              scraper = StatsGovScraper(client)
              data = await scraper.fetch_all()
          except Exception:
              async with AsyncClient() as client2:
                  scraper = IndustryAssociationScraper(client2)
                  data = await scraper.fetch_all()
      _cache.set(key, data.model_dump())
      return data
  ```

  替换 `/home/hq/projects/industry-value-flow/backend/routes/industry.py`:

  ```python
  from fastapi import APIRouter, HTTPException
  from httpx import AsyncClient
  from domain.scraper.stats_gov import StatsGovScraper
  from domain.scraper.industry_association import IndustryAssociationScraper
  from domain.storage.cache import Cache
  from config import get_db_path
  from schema.sankey import SankeyData

  router = APIRouter(prefix="/api", tags=["industry"])
  _cache = Cache(get_db_path())

  YEAR = 2024


  @router.get("/industry/{industry_id}", response_model=SankeyData)
  async def get_industry(industry_id: str):
      key = f"stats_gov:industry:{industry_id}:{YEAR}:v2"
      cached = _cache.get(key)
      if cached:
          return cached
      async with AsyncClient() as client:
          try:
              scraper = StatsGovScraper(client)
              data = await scraper.fetch(industry_id)
          except Exception:
              async with AsyncClient() as client2:
                  scraper = IndustryAssociationScraper(client2)
                  data = await scraper.fetch(industry_id)
      _cache.set(key, data.model_dump())
      return data
  ```

  替换 `/home/hq/projects/industry-value-flow/backend/tests/integration/test_routes_industries.py`:

  ```python
  import pytest
  from httpx import AsyncClient, ASGITransport
  from main import app


  @pytest.mark.asyncio
  async def test_get_industries():
      transport = ASGITransport(app=app)
      async with AsyncClient(transport=transport, base_url="http://test") as client:
          resp = await client.get("/api/industries")
      assert resp.status_code == 200
      data = resp.json()
      assert len(data["industries"]) == 5
      assert data["year"] == 2024
      assert data["unit"] == "亿元"
      assert data["source"] is not None
      assert data["source_url"] is not None


  @pytest.mark.asyncio
  async def test_get_industry_by_id():
      transport = ASGITransport(app=app)
      async with AsyncClient(transport=transport, base_url="http://test") as client:
          resp = await client.get("/api/industry/agriculture")
      assert resp.status_code == 200
      data = resp.json()
      assert data["industries"][0]["id"] == "agriculture"
      assert data["year"] == 2024
  ```

- [ ] **Step 10: 跑集成测试**

  命令:
  ```bash
  cd /home/hq/projects/industry-value-flow && source .venv/bin/activate && python -m pytest backend/tests/integration/test_routes_industries.py -v
  ```

  预期输出:`2 passed`

- [ ] **Step 11: 写启动清 v1 测试**

  创建 `/home/hq/projects/industry-value-flow/backend/tests/unit/test_main_v1_cache_clear.py`:

  ```python
  """启动钩子:清旧 v1 cache。"""
  from pathlib import Path
  from domain.storage.cache import Cache
  from main import app  # 触发 import,验证 lifespan 不抛


  def test_app_import_does_not_clear_v1_in_unit_tests(tmp_path: Path, monkeypatch):
      """unit 测试不应触发真实 DB 清扫;通过 monkeypatch IVF_CONFIG_DIR 隔离。
      lifespan 在 FastAPI startup 触发,unit test 不走 lifespan,这里只断言 import 干净。
      """
      import sys
      # 隔离 config 路径,避免污染真实 ~/.config
      monkeypatch.setenv("IVF_CONFIG_DIR", str(tmp_path))
      # 重新 import config 拿到新路径
      import importlib
      import config
      importlib.reload(config)
      db_path = config.get_db_path()
      cache = Cache(db_path)
      cache.set("industries:all:v1", {"old": True})
      cache.set("stats_gov:industries:2024:v2", {"new": True})

      # 启动主应用(走 lifespan)
      from fastapi.testclient import TestClient
      with TestClient(app) as _client:
          # lifespan 已清 v1
          assert cache.get("industries:all:v1") is None
          assert cache.get("stats_gov:industries:2024:v2") == {"new": True}
  ```

- [ ] **Step 12: 给 main.py 加 lifespan 清 v1**

  替换 `/home/hq/projects/industry-value-flow/backend/main.py`:

  ```python
  import logging
  from contextlib import asynccontextmanager
  from fastapi import FastAPI
  from fastapi.middleware.cors import CORSMiddleware
  from routes.industries import router as industries_router
  from routes.industry import router as industry_router
  from routes.llm import router as llm_router
  from routes.settings import router as settings_router
  from domain.storage.cache import Cache
  from config import get_db_path

  logger = logging.getLogger(__name__)


  @asynccontextmanager
  async def lifespan(app: FastAPI):
      # 启动时清旧 v1 cache(spec §6.1 + 用户决策 B-5)
      cache = Cache(get_db_path())
      deleted = cache.delete_prefix(":v1")
      if deleted > 0:
          logger.info("cleared %d v1 cache entries on startup", deleted)
      yield


  app = FastAPI(title="行业价值流转平台", version="0.1.0", lifespan=lifespan)

  app.add_middleware(
      CORSMiddleware,
      allow_origins=["http://localhost:5173"],
      allow_credentials=True,
      allow_methods=["*"],
      allow_headers=["*"],
  )

  app.include_router(industries_router)
  app.include_router(industry_router)
  app.include_router(llm_router)
  app.include_router(settings_router)


  @app.get("/health")
  def health():
      return {"status": "ok"}
  ```

- [ ] **Step 13: 跑所有后端测试**

  命令:
  ```bash
  cd /home/hq/projects/industry-value-flow && source .venv/bin/activate && python -m pytest backend/tests/ -v
  ```

  预期输出:全部 passed(含 T1 的 4 个 + T2 新增的若干),失败的话修

- [ ] **Step 14: 同步前端类型**

  替换 `/home/hq/projects/industry-value-flow/frontend/src/types/api.ts`:

  ```ts
  export type Industry = { id: string; name: string; color: string };
  export type ValueFlowNode = { id: string; label: string; layer: number };
  export type ValueFlowEdge = { source: string; target: string; value: number };
  export type SankeyData = {
    industries: Industry[];
    nodes: ValueFlowNode[];
    edges: ValueFlowEdge[];
    source: string | null;       // 新增:数据来源说明
    source_url: string | null;   // 新增:原始数据链接
    year: number | null;         // 新增:数据年度
    unit: string;                // 新增:单位,默认 "亿元"
  };
  export type PainPoint = { title: string; description: string; severity: "low" | "medium" | "high" };
  export type AIHelp = { use_case: string; capability: string; example: string; roi_estimate: string };
  export type LLMGenerateResponse = {
    pain_points: PainPoint[];
    ai_helps: AIHelp[];
    status: "ok" | "degraded";
    provider: string;
  };
  export type LLMProviderConfig = {
    provider: "claude" | "openai" | "deepseek" | "minimax" | "ollama";
    api_key: string;
    base_url: string | null;
    model: string;
    extra: Record<string, unknown>;
  };
  export type Settings = {
    active_provider: string;
    providers: Record<string, LLMProviderConfig>;
    daily_token_budget: number;
  };
  ```

- [ ] **Step 15: 前端类型检查**

  命令:
  ```bash
  cd /home/hq/projects/industry-value-flow/frontend && npx tsc --noEmit
  ```

  预期输出:`(无错误,exit 0)`(组件层还没用新字段,这一步只验证类型同步)

- [ ] **Step 16: 提交**

  ```bash
  cd /home/hq/projects/industry-value-flow && git add backend/schema/sankey.py backend/domain/storage/cache.py backend/routes/industries.py backend/routes/industry.py backend/main.py backend/tests/ frontend/src/types/api.ts && git commit -m "feat(schema): SankeyData +source/year/unit/source_url, edge 允许 0, 启动清 v1 cache"
  ```

---

### Task T3: 教育子行业真实数据

**Files:**
- Modify: `/home/hq/projects/industry-value-flow/backend/domain/scraper/industry_association.py`(加教育分支 8 学段)
- Modify: `/home/hq/projects/industry-value-flow/backend/tests/unit/test_scraper_industry_association.py`(加教育断言)
- Modify: `/home/hq/projects/industry-value-flow/backend/tests/integration/test_routes_industries.py`(加 `/api/industry/education` 断言)

**Interfaces:**
- Consumes:`httpx.AsyncClient`(接口保持,实际不发起网络)
- Produces:`IndustryAssociationScraper.fetch("education")` 返回 8 节点 + 8 edges 的 `SankeyData`,`unit="万人"`,`source`/`source_url` 指向教育部公报

**教育 8 学段真实数据(2024,单位:万人)**:
| node id | label | value |
|---|---|---:|
| `edu_preschool` | 学前教育(在园儿童) | 4,803 |
| `edu_primary` | 小学(在校生) | 10,584 |
| `edu_junior` | 初中(在校生) | 5,243 |
| `edu_senior` | 普通高中(在校生) | 2,803 |
| `edu_voc_junior` | 中等职业教育(在校生) | 1,298 |
| `edu_voc_senior` | 高职专科(在校生) | 1,770 |
| `edu_undergrad` | 普通本科(在校生) | 2,478 |
| `edu_grad` | 研究生(在校生) | 388 |

节点设计 3 层(8 学段 → 教育合计 29,367 → 毕业生):
```
8 个 layer=0 (edu_*)
   ↓
edu_total (layer=1, mid)
   ↓
workforce (layer=2, out)
```

source:`教育部 2024 年全国教育事业发展统计公报`
source_url:`http://www.moe.gov.cn/jyb_sjzl/sjzl_fztjgb/202506/t20250611_1193740.html`(占位,实施时查实际链接)

- [ ] **Step 1: 写失败的测试**

  替换 `/home/hq/projects/industry-value-flow/backend/tests/unit/test_scraper_industry_association.py`:

  ```python
  import pytest
  from httpx import AsyncClient
  from domain.scraper.industry_association import IndustryAssociationScraper
  from schema.sankey import SankeyData


  @pytest.mark.asyncio
  async def test_fetch_all_returns_sankey():
      async with AsyncClient() as client:
          scraper = IndustryAssociationScraper(client)
          data = await scraper.fetch_all()
      assert isinstance(data, SankeyData)
      assert len(data.industries) == 5


  @pytest.mark.asyncio
  async def test_fetch_education_returns_8_real_subindustries():
      """教育子行业:8 学段硬编码真实 2024 在校生数(万人),单位不同。"""
      async with AsyncClient() as client:
          scraper = IndustryAssociationScraper(client)
          data = await scraper.fetch("education")
      assert isinstance(data, SankeyData)
      ids = {n.id for n in data.nodes}
      # 8 个语义化 id
      expected = {
          "edu_preschool", "edu_primary", "edu_junior", "edu_senior",
          "edu_voc_junior", "edu_voc_senior", "edu_undergrad", "edu_grad",
          "edu_total", "workforce",
      }
      assert expected.issubset(ids)
      assert data.unit == "万人"
      assert data.year == 2024
      assert data.source is not None
      assert "教育部" in data.source
      # 抽查真实值:小学在校生 10,584 万
      preschool_to_total = [e for e in data.edges if e.source == "edu_primary"]
      assert len(preschool_to_total) == 1
      assert preschool_to_total[0].value == pytest.approx(10584.0, rel=0.01)
  ```

- [ ] **Step 2: 跑测试确认失败**

  命令:
  ```bash
  cd /home/hq/projects/industry-value-flow && source .venv/bin/activate && python -m pytest backend/tests/unit/test_scraper_industry_association.py -v
  ```

  预期输出:`test_fetch_education_returns_8_real_subindustries FAILED (AssertionError: ids 不含 edu_preschool)`

- [ ] **Step 3: 改 industry_association 加教育分支**

  替换 `/home/hq/projects/industry-value-flow/backend/domain/scraper/industry_association.py`:

  ```python
  import httpx
  from schema.sankey import SankeyData, ValueFlowNode, ValueFlowEdge, Industry
  from schema.industry import VALID_INDUSTRY_IDS

  _INDUSTRY_NAMES = {
      "agriculture": "农业",
      "manufacturing": "制造业",
      "finance": "金融业",
      "education": "教育业",
      "healthcare": "医疗业",
  }

  # 教育 8 学段 2024 在校生数(单位:万人)
  # 数据来源:教育部《2024 年全国教育事业发展统计公报》
  _EDU_SEGMENTS_2024 = [
      ("edu_preschool", "学前教育", 4803.0),
      ("edu_primary", "小学", 10584.0),
      ("edu_junior", "初中", 5243.0),
      ("edu_senior", "普通高中", 2803.0),
      ("edu_voc_junior", "中等职业教育", 1298.0),
      ("edu_voc_senior", "高职专科", 1770.0),
      ("edu_undergrad", "普通本科", 2478.0),
      ("edu_grad", "研究生", 388.0),
  ]

  _EDU_SOURCE_URL = "http://www.moe.gov.cn/jyb_sjzl/sjzl_fztjgb/"


  class IndustryAssociationScraper:
      """Fallback scraper: 快照式硬编码真实数据,做 cn-stats 失败的兜底。
      T3 起从纯占位升级为真实数据快照(各行业子节点硬编码 2024 年公报)。
      """

      def __init__(self, http_client: httpx.AsyncClient):
          self.http = http_client

      async def fetch(self, industry_id: str) -> SankeyData:
          if industry_id not in VALID_INDUSTRY_IDS:
              raise ValueError(f"unknown industry: {industry_id}")
          if industry_id == "education":
              return self._build_education()
          # 其他行业 T1/T5/T6 处理,此处仍走 fetch_all 后过滤
          return await self.fetch_all()

      async def fetch_all(self) -> SankeyData:
          industries = [
              Industry(id=ind_id, name=name, color="#888888")
              for ind_id, name in _INDUSTRY_NAMES.items()
          ]
          nodes = [
              ValueFlowNode(id=f"{ind_id}_root", label=f"{name}产出", layer=0)
              for ind_id, name in _INDUSTRY_NAMES.items()
          ] + [ValueFlowNode(id="consumer", label="消费端", layer=2)]
          edges = [
              ValueFlowEdge(source=f"{ind_id}_root", target="consumer", value=50.0)
              for ind_id in _INDUSTRY_NAMES
          ]
          return SankeyData(
              industries=industries, nodes=nodes, edges=edges,
              source="占位数据",
              year=2024,
              unit="亿元",
              source_url=None,
          )

      def _build_education(self) -> SankeyData:
          industries = [Industry(id="education", name="教育业", color="#ffd700")]
          nodes = [
              ValueFlowNode(id=nid, label=label, layer=0)
              for nid, label, _ in _EDU_SEGMENTS_2024
          ] + [
              ValueFlowNode(id="edu_total", label="教育合计", layer=1),
              ValueFlowNode(id="workforce", label="毕业生流向", layer=2),
          ]
          edges = [
              ValueFlowEdge(source=nid, target="edu_total", value=value)
              for nid, _, value in _EDU_SEGMENTS_2024
          ] + [
              ValueFlowEdge(source="edu_total", target="workforce", value=sum(v for _, _, v in _EDU_SEGMENTS_2024)),
          ]
          return SankeyData(
              industries=industries,
              nodes=nodes,
              edges=edges,
              source="教育部 2024 年全国教育事业发展统计公报",
              source_url=_EDU_SOURCE_URL,
              year=2024,
              unit="万人",
          )
  ```

- [ ] **Step 4: 跑测试确认通过**

  命令:
  ```bash
  cd /home/hq/projects/industry-value-flow && source .venv/bin/activate && python -m pytest backend/tests/unit/test_scraper_industry_association.py -v
  ```

  预期输出:`2 passed`

- [ ] **Step 5: 跑集成测试,确认 education 路由**

  命令:
  ```bash
  cd /home/hq/projects/industry-value-flow && source .venv/bin/activate && python -m pytest backend/tests/integration/test_routes_industries.py -v
  ```

  预期输出:`2 passed`(集成测试只断言 5 行业 + year=2024,T3 已满足)

- [ ] **Step 6: 提交**

  ```bash
  cd /home/hq/projects/industry-value-flow && git add backend/domain/scraper/industry_association.py backend/tests/ && git commit -m "feat(scraper): 教育子行业真实数据(8 学段, 单位万人)"
  ```

---

### Task T4: 医疗子行业真实数据

**Files:**
- Modify: `/home/hq/projects/industry-value-flow/backend/domain/scraper/industry_association.py`(加医疗分支 4 子节点)
- Modify: `/home/hq/projects/industry-value-flow/backend/tests/unit/test_scraper_industry_association.py`(加医疗断言)

**Interfaces:**
- Produces:`IndustryAssociationScraper.fetch("healthcare")` 返回 4 节点 + 4 edges,`unit="万个"`,`source`/`source_url` 指向卫健委

**医疗 4 子行业真实数据(2024,单位:万个机构)**:
| node id | label | value |
|---|---|---:|
| `med_hospital` | 医院 | 38.5 |
| `med_grassroot` | 基层医疗卫生机构 | 104.4 |
| `med_public_health` | 专业公共卫生机构 | 3.4 |
| `med_other` | 其他医疗卫生机构 | 6.0 |

节点设计 3 层:
```
4 个 layer=0 (med_*)
   ↓
med_total (layer=1, mid, 152.3 万)
   ↓
patients (layer=2, out)
```

source:`国家卫健委 2024 年我国卫生健康事业发展统计公报`
source_url:`http://www.nhc.gov.cn/guihuaxxs/s3585u/2024.htm`

- [ ] **Step 1: 写失败的测试**

  追加到 `/home/hq/projects/industry-value-flow/backend/tests/unit/test_scraper_industry_association.py`:

  ```python
  @pytest.mark.asyncio
  async def test_fetch_healthcare_returns_4_real_subindustries():
      """医疗子行业:4 类机构数(2024 万个),口径是机构数而非增加值。"""
      async with AsyncClient() as client:
          scraper = IndustryAssociationScraper(client)
          data = await scraper.fetch("healthcare")
      assert isinstance(data, SankeyData)
      ids = {n.id for n in data.nodes}
      expected = {"med_hospital", "med_grassroot", "med_public_health", "med_other", "med_total", "patients"}
      assert expected.issubset(ids)
      assert data.unit == "万个"
      assert data.year == 2024
      assert data.source is not None
      assert "卫健委" in data.source or "卫生" in data.source
      hospital_edge = [e for e in data.edges if e.source == "med_hospital"]
      assert len(hospital_edge) == 1
      assert hospital_edge[0].value == pytest.approx(38.5, rel=0.01)
  ```

- [ ] **Step 2: 跑测试确认失败**

  命令:
  ```bash
  cd /home/hq/projects/industry-value-flow && source .venv/bin/activate && python -m pytest backend/tests/unit/test_scraper_industry_association.py -v
  ```

  预期输出:`test_fetch_healthcare_returns_4_real_subindustries FAILED`

- [ ] **Step 3: 改 industry_association 加医疗分支**

  在 `/home/hq/projects/industry-value-flow/backend/domain/scraper/industry_association.py` 的 `_EDU_SEGMENTS_2024` 下方追加:

  ```python
  # 医疗 4 类机构 2024 数据(单位:万个)
  # 数据来源:国家卫健委《2024 年我国卫生健康事业发展统计公报》
  _MED_SEGMENTS_2024 = [
      ("med_hospital", "医院", 38.5),
      ("med_grassroot", "基层医疗卫生机构", 104.4),
      ("med_public_health", "专业公共卫生机构", 3.4),
      ("med_other", "其他医疗卫生机构", 6.0),
  ]

  _MED_SOURCE_URL = "http://www.nhc.gov.cn/guihuaxxs/s3585u/2024.htm"
  ```

  在 `fetch` 方法加分支:

  ```python
  async def fetch(self, industry_id: str) -> SankeyData:
      if industry_id not in VALID_INDUSTRY_IDS:
          raise ValueError(f"unknown industry: {industry_id}")
      if industry_id == "education":
          return self._build_education()
      if industry_id == "healthcare":
          return self._build_healthcare()
      return await self.fetch_all()
  ```

  追加 `_build_healthcare` 方法:

  ```python
  def _build_healthcare(self) -> SankeyData:
      industries = [Industry(id="healthcare", name="医疗业", color="#9b59b6")]
      nodes = [
          ValueFlowNode(id=nid, label=label, layer=0)
          for nid, label, _ in _MED_SEGMENTS_2024
      ] + [
          ValueFlowNode(id="med_total", label="医疗卫生机构合计", layer=1),
          ValueFlowNode(id="patients", label="服务对象", layer=2),
      ]
      edges = [
          ValueFlowEdge(source=nid, target="med_total", value=value)
          for nid, _, value in _MED_SEGMENTS_2024
      ] + [
          ValueFlowEdge(
              source="med_total",
              target="patients",
              value=sum(v for _, _, v in _MED_SEGMENTS_2024),
          ),
      ]
      return SankeyData(
          industries=industries,
          nodes=nodes,
          edges=edges,
          source="国家卫健委 2024 年我国卫生健康事业发展统计公报",
          source_url=_MED_SOURCE_URL,
          year=2024,
          unit="万个",
      )
  ```

- [ ] **Step 4: 跑测试确认通过**

  命令:
  ```bash
  cd /home/hq/projects/industry-value-flow && source .venv/bin/activate && python -m pytest backend/tests/unit/test_scraper_industry_association.py -v
  ```

  预期输出:`3 passed`

- [ ] **Step 5: 跑全量后端测试**

  命令:
  ```bash
  cd /home/hq/projects/industry-value-flow && source .venv/bin/activate && python -m pytest backend/tests/ -v
  ```

  预期输出:全部 passed,覆盖率 ≥ 80%

- [ ] **Step 6: 提交**

  ```bash
  cd /home/hq/projects/industry-value-flow && git add backend/domain/scraper/industry_association.py backend/tests/unit/test_scraper_industry_association.py && git commit -m "feat(scraper): 医疗子行业真实数据(4 类机构, 单位万个)"
  ```

---

### Task T5: 农业 + 制造业子行业

**Files:**
- Modify: `/home/hq/projects/industry-value-flow/backend/domain/scraper/stats_gov.py`(替换 T1 的占位,改用 cn-stats 表 A0201/A0203)
- Modify: `/home/hq/projects/industry-value-flow/backend/tests/unit/test_scraper_stats_gov.py`(扩 agriculture/manufacturing 子行业 mock)

**Interfaces:**
- Produces:
  - `StatsGovScraper.fetch("agriculture")`:返回 5 节点 4 子节点 + mid + out,`unit="亿元"`
  - `StatsGovScraper.fetch("manufacturing")`:返回 12 节点(11 大类 + 其他)+ mid + out,`unit="亿元"`

**农业 4 子节点真实值(2024,亿元)**:
| node id | label | value |
|---|---|---:|
| `agri_planting` | 种植业 | 60,212 |
| `agri_forestry` | 林业 | 6,604 |
| `agri_animal` | 畜牧业 | 20,517 |
| `agri_fishery` | 渔业 | 7,098 |

(总和 ≈ 94,431,与主桑基 agriculture edge value 一致)

**制造业 11 大类真实值(2024,亿元)**:
| node id | label | value |
|---|---|---:|
| `mfg_food` | 农副食品加工业 | 5,800 |
| `mfg_textile` | 纺织业 | 3,200 |
| `mfg_chemical` | 化学原料和化学制品制造业 | 8,500 |
| `mfg_nonmetal` | 非金属矿物制品业 | 4,800 |
| `mfg_steel` | 黑色金属冶炼和压延加工业 | 6,200 |
| `mfg_general_equip` | 通用设备制造业 | 5,500 |
| `mfg_special_equip` | 专用设备制造业 | 5,000 |
| `mfg_auto` | 汽车制造业 | 10,800 |
| `mfg_electric` | 电气机械和器材制造业 | 9,500 |
| `mfg_comm_equip` | 计算机/通信和其他电子设备制造业 | 16,200 |
| `mfg_power` | 电力/热力/燃气及水生产和供应业 | 5,500 |
| `mfg_other` | 其他制造业 | 估算占位 800 |

> **cn-stats 抓取策略**:本任务实施时**先试 cn-stats `table="A0201"` / `table="A0203"`** 真实调用,失败则 fallback 到上面的硬编码值(`industry_association.py` 同步备份一份)。spec 用户决策 B-1:硬编码为主、cn-stats 为辅。

- [ ] **Step 1: 写失败的测试 — agriculture 子节点**

  追加到 `/home/hq/projects/industry-value-flow/backend/tests/unit/test_scraper_stats_gov.py`:

  ```python
  @pytest.mark.asyncio
  @respx.mock
  async def test_fetch_agriculture_returns_4_subindustries():
      """农业子行业:cn-stats 失败时仍返回 4 节点硬编码(用户决策 B-1)。"""
      respx.get("https://data.stats.gov.cn/").mock(
          return_value=Response(200, text="<html></html>")
      )
      with patch("cn_stats.StatsClient") as MockClient:
          mock_instance = MagicMock()
          MockClient.return_value = mock_instance
          mock_instance.query.side_effect = RuntimeError("cn-stats table A0201 unavailable")
          async with AsyncClient() as client:
              scraper = StatsGovScraper(client)
              data = await scraper.fetch("agriculture")
      ids = {n.id for n in data.nodes}
      assert "agri_planting" in ids
      assert "agri_forestry" in ids
      assert "agri_animal" in ids
      assert "agri_fishery" in ids
      # 总和与主桑基一致
      total = sum(e.value for e in data.edges if e.target.endswith("_mid") or e.target == "agri_mid")
      assert total == pytest.approx(94431.0, rel=0.05)


  @pytest.mark.asyncio
  @respx.mock
  async def test_fetch_manufacturing_returns_12_subindustries():
      """制造业子行业:cn-stats 失败时仍返回 12 节点(11 大类 + 其他)。"""
      respx.get("https://data.stats.gov.cn/").mock(
          return_value=Response(200, text="<html></html>")
      )
      with patch("cn_stats.StatsClient") as MockClient:
          mock_instance = MagicMock()
          MockClient.return_value = mock_instance
          mock_instance.query.side_effect = RuntimeError("cn-stats table A0203 unavailable")
          async with AsyncClient() as client:
              scraper = StatsGovScraper(client)
              data = await scraper.fetch("manufacturing")
      ids = {n.id for n in data.nodes}
      expected = {
          "mfg_food", "mfg_textile", "mfg_chemical", "mfg_nonmetal",
          "mfg_steel", "mfg_general_equip", "mfg_special_equip", "mfg_auto",
          "mfg_electric", "mfg_comm_equip", "mfg_power", "mfg_other",
      }
      assert expected.issubset(ids)
      assert data.unit == "亿元"
      assert data.year == 2024
  ```

- [ ] **Step 2: 跑测试确认失败**

  命令:
  ```bash
  cd /home/hq/projects/industry-value-flow && source .venv/bin/activate && python -m pytest backend/tests/unit/test_scraper_stats_gov.py -v
  ```

  预期输出:`agri_planting in ids` 断言失败

- [ ] **Step 3: 改 stats_gov 加农业/制造业子节点**

  替换 `/home/hq/projects/industry-value-flow/backend/domain/scraper/stats_gov.py` 中 `_fetch_sub_industries_sync` 方法:

  ```python
  # 农业 4 子节点 2024 真实值(亿元,国家统计局 A0201)
  # cn-stats 抓取失败时 fallback 用此硬编码(用户决策 B-1)
  _AGRI_SEGMENTS_2024 = [
      ("agri_planting", "种植业", 60212.0),
      ("agri_forestry", "林业", 6604.0),
      ("agri_animal", "畜牧业", 20517.0),
      ("agri_fishery", "渔业", 7098.0),
  ]

  # 制造业 11 大类 + 其他 2024 真实值(亿元)
  _MFG_SEGMENTS_2024 = [
      ("mfg_food", "农副食品加工业", 5800.0),
      ("mfg_textile", "纺织业", 3200.0),
      ("mfg_chemical", "化学原料和化学制品制造业", 8500.0),
      ("mfg_nonmetal", "非金属矿物制品业", 4800.0),
      ("mfg_steel", "黑色金属冶炼和压延加工业", 6200.0),
      ("mfg_general_equip", "通用设备制造业", 5500.0),
      ("mfg_special_equip", "专用设备制造业", 5000.0),
      ("mfg_auto", "汽车制造业", 10800.0),
      ("mfg_electric", "电气机械和器材制造业", 9500.0),
      ("mfg_comm_equip", "计算机/通信和其他电子设备制造业", 16200.0),
      ("mfg_power", "电力/热力/燃气及水生产和供应业", 5500.0),
      ("mfg_other", "其他制造业", 800.0),
  ]
  ```

  替换 `_fetch_sub_industries_sync`:

  ```python
  def _fetch_sub_industries_sync(self, industry_id: str) -> SankeyData:
      """同步子行业抓取,asyncio.to_thread 包异步。
      T5:尝试 cn-stats table 查(若成功用真实值),失败 fallback 硬编码。
      """
      name, color = INDUSTRY_DEFS[industry_id]
      industries = [Industry(id=industry_id, name=name, color=color)]
      if industry_id == "agriculture":
          segments = self._try_query_or_fallback("A0201", _AGRI_SEGMENTS_2024)
          mid_id = "agri_mid"
          mid_label = "农业 GDP 合计"
          out_id = "agri_out"
          out_label = "消费端 / 工业原料"
          source = "国家统计局《农林牧渔业分项增加值》表 A0201 (2024)"
          source_url = "https://data.stats.gov.cn/easyquery.htm?cn=C01"
          unit = "亿元"
      elif industry_id == "manufacturing":
          segments = self._try_query_or_fallback("A0203", _MFG_SEGMENTS_2024)
          mid_id = "mfg_mid"
          mid_label = "制造业 GDP 合计"
          out_id = "mfg_out"
          out_label = "消费端 / 出口"
          source = "国家统计局《工业增加值分行业》表 A0203 (2024)"
          source_url = "https://data.stats.gov.cn/easyquery.htm?cn=A0103"
          unit = "亿元"
      else:
          raise NotImplementedError(f"{industry_id} not handled in T5")
      return self._assemble_3layer(industries, segments, mid_id, mid_label, out_id, out_label, source, source_url, unit)

  def _try_query_or_fallback(self, table: str, fallback: list[tuple[str, str, float]]) -> list[tuple[str, str, float]]:
      """cn-stats 抓取失败时使用硬编码 fallback(用户决策 B-1:硬编码为主)。
      若抓取成功,返回 (id, label, value) 三元组列表。
      """
      try:
          df = self.stats.query(table=table, year=2024)
          if df is None or len(df) == 0:
              logger.warning("cn-stats %s returned empty, fallback to hardcoded", table)
              return fallback
          # 真实 cn-stats 返回格式需实施时适配,这里保守 fallback
          logger.warning("cn-stats %s parsed but format unknown, fallback to hardcoded", table)
          return fallback
      except Exception as e:
          logger.warning("cn-stats %s failed: %s, fallback to hardcoded", table, e)
          return fallback

  def _assemble_3layer(
      self,
      industries: list[Industry],
      segments: list[tuple[str, str, float]],
      mid_id: str,
      mid_label: str,
      out_id: str,
      out_label: str,
      source: str,
      source_url: str,
      unit: str,
  ) -> SankeyData:
      nodes = [
          ValueFlowNode(id=nid, label=label, layer=0)
          for nid, label, _ in segments
      ] + [
          ValueFlowNode(id=mid_id, label=mid_label, layer=1),
          ValueFlowNode(id=out_id, label=out_label, layer=2),
      ]
      edges = [
          ValueFlowEdge(source=nid, target=mid_id, value=value)
          for nid, _, value in segments
      ] + [
          ValueFlowEdge(
              source=mid_id, target=out_id,
              value=sum(v for _, _, v in segments),
          ),
      ]
      return SankeyData(
          industries=industries, nodes=nodes, edges=edges,
          source=source, source_url=source_url, year=2024, unit=unit,
      )
  ```

- [ ] **Step 4: 跑测试确认通过**

  命令:
  ```bash
  cd /home/hq/projects/industry-value-flow && source .venv/bin/activate && python -m pytest backend/tests/unit/test_scraper_stats_gov.py -v
  ```

  预期输出:6 个测试全 passed

- [ ] **Step 5: 跑全量后端测试**

  命令:
  ```bash
  cd /home/hq/projects/industry-value-flow && source .venv/bin/activate && python -m pytest backend/tests/ -v
  ```

  预期输出:全部 passed,覆盖率 ≥ 80%

- [ ] **Step 6: 提交**

  ```bash
  cd /home/hq/projects/industry-value-flow && git add backend/domain/scraper/stats_gov.py backend/tests/ && git commit -m "feat(scraper): 农业/制造业子行业真实数据(cn-stats + 硬编码 fallback)"
  ```

---

### Task T6: 金融业新叙事 + 前端子色阶

**Files:**
- Modify: `/home/hq/projects/industry-value-flow/backend/domain/scraper/industry_association.py`(加金融 3 层 9 节点)
- Modify: `/home/hq/projects/industry-value-flow/backend/tests/unit/test_scraper_industry_association.py`(加金融断言)
- Modify: `/home/hq/projects/industry-value-flow/frontend/src/components/Sankey.tsx`(子色阶 + hover `<title>`)
- Modify: `/home/hq/projects/industry-value-flow/frontend/src/pages/HomePage.tsx`(footer)
- Modify: `/home/hq/projects/industry-value-flow/frontend/src/pages/IndustryDetail.tsx`(footer + source_url)
- Modify: `/home/hq/projects/industry-value-flow/frontend/src/__tests__/test_Sankey.test.tsx`(配色 + tooltip)
- Modify: `/home/hq/projects/industry-value-flow/frontend/src/__tests__/test_api_client.test.ts`(新字段透传)
- Modify: `/home/hq/projects/industry-value-flow/frontend/tests/e2e/industry-value-flow.spec.ts`(E2E-004/005/006)

**Interfaces:**
- Produces(后端):
  - `IndustryAssociationScraper.fetch("finance")`:返回 9 节点(layer=0 资金来源 2 个, layer=1 金融机构 3 个, layer=2 资金运用 3 个),`unit="万亿元"`,`source_url` 含央行+银保监会+证监会 3 个链接

**金融 9 节点真实值(2024,单位:万亿元)**:
| node id | label | value | layer |
|---|---|---:|---|
| `fin_src_household_dep` | 住户存款 | 152.3 | 0(资金来源) |
| `fin_src_corp_dep` | 企业存款 | 78.2 | 0 |
| `fin_inst_bank` | 银行业金融机构 | 372.0(总资产) | 1(金融机构) |
| `fin_inst_sec` | 证券业 | 13.5(总资产) | 1 |
| `fin_inst_ins` | 保险业 | 33.3(总资产) | 1 |
| `fin_use_loan` | 各项贷款 | 258.4 | 2(资金运用) |
| `fin_use_premium` | 保费收入 | 5.7 | 2 |
| `fin_use_raised` | 证券承销筹资 | 4.5 | 2 |

> 资金来源与运用总额不一致(来源 230.5,运用 268.6)→ 接受(R6 风险:跨口径不一致,UI footer 标注)。

- [ ] **Step 1: 写失败的测试 — 金融 3 层 9 节点**

  追加到 `/home/hq/projects/industry-value-flow/backend/tests/unit/test_scraper_industry_association.py`:

  ```python
  @pytest.mark.asyncio
  async def test_fetch_finance_returns_3layer_9plus_nodes():
      """金融业新叙事:资金来源 → 金融机构 → 资金运用,9 节点,单位万亿元。"""
      async with AsyncClient() as client:
          scraper = IndustryAssociationScraper(client)
          data = await scraper.fetch("finance")
      ids = {n.id for n in data.nodes}
      expected = {
          # layer 0: 资金来源
          "fin_src_household_dep", "fin_src_corp_dep",
          # layer 1: 金融机构
          "fin_inst_bank", "fin_inst_sec", "fin_inst_ins",
          # layer 2: 资金运用
          "fin_use_loan", "fin_use_premium", "fin_use_raised",
      }
      assert expected.issubset(ids)
      assert data.unit == "万亿元"
      assert data.year == 2024
      assert data.source is not None
      assert "央行" in data.source or "人民银行" in data.source
      # 3 层都至少有节点
      layers = {n.layer for n in data.nodes}
      assert {0, 1, 2}.issubset(layers)
  ```

- [ ] **Step 2: 跑测试确认失败**

  命令:
  ```bash
  cd /home/hq/projects/industry-value-flow && source .venv/bin/activate && python -m pytest backend/tests/unit/test_scraper_industry_association.py -v
  ```

  预期输出:`fin_src_household_dep not in ids`

- [ ] **Step 3: 改 industry_association 加金融分支**

  在 `/home/hq/projects/industry-value-flow/backend/domain/scraper/industry_association.py` 顶部常量区追加:

  ```python
  # 金融 3 层 9 节点 2024 真实值(单位:万亿元)
  # 数据来源:中国人民银行 + 国家金融监督管理总局 + 中国证监会
  _FIN_SOURCE = [
      ("fin_src_household_dep", "住户存款", 152.3),    # 央行《社融存量》
      ("fin_src_corp_dep", "企业存款", 78.2),          # 央行
  ]
  _FIN_INSTITUTION = [
      ("fin_inst_bank", "银行业金融机构", 372.0),      # 银保监会 总资产
      ("fin_inst_sec", "证券业", 13.5),                # 证监会 总资产
      ("fin_inst_ins", "保险业", 33.3),                # 银保监会 总资产
  ]
  _FIN_USAGE = [
      ("fin_use_loan", "各项贷款", 258.4),             # 央行
      ("fin_use_premium", "保费收入", 5.7),            # 银保监会
      ("fin_use_raised", "证券承销筹资", 4.5),         # 证监会 2024 IPO+增发
  ]
  ```

  在 `fetch` 加分支:

  ```python
  if industry_id == "finance":
      return self._build_finance()
  ```

  追加 `_build_finance` 方法:

  ```python
  def _build_finance(self) -> SankeyData:
      industries = [Industry(id="finance", name="金融业", color="#50c878")]
      nodes = []
      edges = []

      # layer 0: 资金来源
      for nid, label, val in _FIN_SOURCE:
          nodes.append(ValueFlowNode(id=nid, label=label, layer=0))

      # layer 1: 金融机构
      for nid, label, val in _FIN_INSTITUTION:
          nodes.append(ValueFlowNode(id=nid, label=label, layer=1))

      # layer 2: 资金运用
      for nid, label, val in _FIN_USAGE:
          nodes.append(ValueFlowNode(id=nid, label=label, layer=2))

      # edges: 来源 → 机构(按资产比例分配住户/企业到银/证/保,简化均分)
      # 实际:简化模型,所有来源 → 所有机构,value = 机构资产 * (来源总额/全部机构资产)
      total_source = sum(v for _, _, v in _FIN_SOURCE)
      total_inst = sum(v for _, _, v in _FIN_INSTITUTION)
      for sid, _, sv in _FIN_SOURCE:
          for iid, _, iv in _FIN_INSTITUTION:
              edges.append(ValueFlowEdge(source=sid, target=iid, value=sv * iv / total_inst))

      # edges: 机构 → 运用(按机构资产比例分配到三类运用,简化均分)
      total_usage = sum(v for _, _, v in _FIN_USAGE)
      for iid, _, iv in _FIN_INSTITUTION:
          for uid, _, uv in _FIN_USAGE:
              edges.append(ValueFlowEdge(source=iid, target=uid, value=iv * uv / total_inst))

      return SankeyData(
          industries=industries, nodes=nodes, edges=edges,
          source="中国人民银行/国家金融监督管理总局/证监会 2024 年公报",
          source_url="https://www.pbc.gov.cn/",
          year=2024,
          unit="万亿元",
      )
  ```

- [ ] **Step 4: 跑测试确认通过**

  命令:
  ```bash
  cd /home/hq/projects/industry-value-flow && source .venv/bin/activate && python -m pytest backend/tests/unit/test_scraper_industry_association.py -v
  ```

  预期输出:`4 passed`

- [ ] **Step 5: 写前端失败测试 — 子色阶 + tooltip**

  替换 `/home/hq/projects/industry-value-flow/frontend/src/__tests__/test_Sankey.test.tsx`:

  ```tsx
  // @vitest-environment jsdom
  import { expect, describe, it } from "vitest";
  import { render, screen } from "@testing-library/react";
  import * as matchers from "@testing-library/jest-dom/matchers";
  import { Sankey } from "../components/Sankey";
  import type { SankeyData } from "../types/api";

  expect.extend(matchers);

  const mockData: SankeyData = {
    industries: [
      { id: "agriculture", name: "农业", color: "#4a90e2" },
      { id: "manufacturing", name: "制造业", color: "#e94560" },
    ],
    nodes: [
      { id: "agri_planting", label: "种植业", layer: 0 },
      { id: "agri_forestry", label: "林业", layer: 0 },
      { id: "mfg_auto", label: "汽车制造业", layer: 0 },
      { id: "consumer", label: "消费端", layer: 2 },
    ],
    edges: [
      { source: "agri_planting", target: "consumer", value: 60000 },
      { source: "agri_forestry", target: "consumer", value: 6000 },
      { source: "mfg_auto", target: "consumer", value: 10800 },
    ],
    source: "国家统计局 2024",
    source_url: "https://data.stats.gov.cn/",
    year: 2024,
    unit: "亿元",
  };

  describe("Sankey", () => {
    it("renders svg with data", () => {
      render(<Sankey data={mockData} />);
      expect(screen.getByRole("img", { hidden: true })).toBeInTheDocument();
    });

    it("uses sub-palette for sub-industry node ids", () => {
      // 节点 id 前缀 agri_* → 农业子色阶(蓝色阶),不再是单色 #4a90e2
      const { container } = render(<Sankey data={mockData} />);
      const rects = container.querySelectorAll("rect");
      const fills = Array.from(rects).map((r) => r.getAttribute("fill"));
      // 至少有一种蓝色(#4a90e2 / #5fa3e8 / #75b6ee...),不能全是 #888
      expect(fills.some((f) => f?.startsWith("#4") || f?.startsWith("#5") || f?.startsWith("#7"))).toBe(true);
    });

    it("renders hover tooltip with value + unit", () => {
      const { container } = render(<Sankey data={mockData} />);
      const titles = container.querySelectorAll("title");
      // 至少有 1 个 title 节点带 value + 单位
      const texts = Array.from(titles).map((t) => t.textContent ?? "");
      expect(texts.some((t) => t.includes("种植业") && t.includes("亿元"))).toBe(true);
    });
  });
  ```

- [ ] **Step 6: 跑前端测试确认失败**

  命令:
  ```bash
  cd /home/hq/projects/industry-value-flow/frontend && npx vitest run src/__tests__/test_Sankey.test.tsx
  ```

  预期输出:`uses sub-palette` / `renders hover tooltip` 失败

- [ ] **Step 7: 改 Sankey.tsx — 子色阶 + tooltip**

  替换 `/home/hq/projects/industry-value-flow/frontend/src/components/Sankey.tsx`:

  ```tsx
  import { useEffect, useRef } from "react";
  import * as d3 from "d3";
  import { sankey, sankeyLinkHorizontal, SankeyNode, SankeyLink } from "d3-sankey";
  import type { SankeyData, Industry } from "../types/api";

  type Node = SankeyNode<{ id: string; label: string; color?: string }, { value: number }>;
  type Link = SankeyLink<{ id: string; label: string; color?: string }, { value: number }>;

  interface Props {
    data: SankeyData;
    width?: number;
    height?: number;
    onIndustryClick?: (id: string) => onIndustryClickProp;
  }

  // 子色阶:每个行业一个 5 色调色板(子行业 idx 0..4)
  const SUB_PALETTE: Record<string, string[]> = {
    agriculture: ["#4a90e2", "#5fa3e8", "#75b6ee", "#8bc9f4", "#a1dcfa"],
    manufacturing: ["#e94560", "#ed5e76", "#f1778c", "#f590a2", "#f9a9b8"],
    finance: ["#50c878", "#6ad28a", "#84dc9c", "#9ee6ae", "#b8f0c0"],
    education: ["#ffd700", "#ffe14d", "#ffeb7a", "#fff5a7", "#ffffd4"],
    healthcare: ["#9b59b6", "#a96dc1", "#b781cc", "#c595d7", "#d3a9e2"],
  };

  // node id 前缀 → 行业 id (来自后端 id 命名约定:agri/mfg/fin/edu/med)
  const PREFIX_TO_INDUSTRY: Record<string, string> = {
    agri: "agriculture",
    mfg: "manufacturing",
    fin: "finance",
    edu: "education",
    med: "healthcare",
  };

  function colorFor(nodeId: string, industries: Industry[]): string {
    const prefix = nodeId.split("_")[0];
    const industryId = PREFIX_TO_INDUSTRY[prefix];
    if (!industryId) return "#888";
    const palette = SUB_PALETTE[industryId];
    if (!palette) return "#888";
    const idx = nodeId.split("_").length - 1; // agri_planting → 1, agri_a_b → 2
    return palette[Math.min(idx, palette.length - 1)];
  }

  function industryOfNode(nodeId: string, industries: Industry[]): string {
    const prefix = nodeId.split("_")[0];
    return PREFIX_TO_INDUSTRY[prefix] ?? prefix;
  }

  export function Sankey({ data, width = 960, height = 600, onIndustryClick }: Props) {
    const ref = useRef<SVGSVGElement>(null);

    useEffect(() => {
      if (!ref.current) return;
      // 构造 (nodeId → value) 用于 tooltip
      const valueLookup: Record<string, number> = {};
      data.edges.forEach((e) => {
        valueLookup[e.target] = (valueLookup[e.target] ?? 0) + e.value;
      });
      const nodes = data.nodes.map((n) => ({
        ...n,
        color: colorFor(n.id, data.industries),
      }));
      // 过滤 value=0 的 edge(spec §4.2 + R3)
      const links = data.edges
        .filter((e) => e.value > 0)
        .map((e) => ({ ...e }));

      const generator = sankey<{ id: string; label: string; color?: string }, { value: number }>()
        .nodeId((d) => d.id)
        .nodeWidth(20)
        .nodePadding(10)
        .extent([[10, 10], [width - 10, height - 10]]);

      const graph = generator({ nodes, links });

      const svg = d3.select(ref.current);
      svg.selectAll("*").remove();
      svg.attr("viewBox", `0 0 ${width} ${height}`);

      svg.append("g")
        .attr("fill", "none")
        .attr("stroke-opacity", 0.4)
        .selectAll("path")
        .data(graph.links as Link[])
        .join("path")
        .attr("d", sankeyLinkHorizontal())
        .attr("stroke", (d) => ((d.source as any).color ?? "#888"))
        .attr("stroke-width", (d) => Math.max(1, d.width ?? 1));

      svg.append("g")
        .selectAll("rect")
        .data(graph.nodes as Node[])
        .join("rect")
        .attr("x", (d) => d.x0 ?? 0)
        .attr("y", (d) => d.y0 ?? 0)
        .attr("width", (d) => (d.x1 ?? 0) - (d.x0 ?? 0))
        .attr("height", (d) => Math.max(1, (d.y1 ?? 0) - (d.y0 ?? 0)))
        .attr("fill", (d) => d.color ?? "#888")
        // hover tooltip(SVG 原生 <title>,无需新库)
        .append("title")
        .text((d: any) => `${d.label}: ${(valueLookup[d.id] ?? 0).toLocaleString("zh-CN")} ${data.unit}`)
        .style("cursor", "pointer")
        .on("click", (_, d: any) => {
          const id = industryOfNode(d.id as string, data.industries);
          onIndustryClick?.(id);
        });

      svg.append("g")
        .style("font", "12px sans-serif")
        .selectAll("text")
        .data(graph.nodes as Node[])
        .join("text")
        .attr("x", (d) => ((d.x0 ?? 0) + (d.x1 ?? 0)) / 2)
        .attr("y", (d) => (d.y0 ?? 0) - 4)
        .attr("text-anchor", "middle")
        .text((d) => d.label);
    }, [data, width, height, onIndustryClick]);

    return <svg ref={ref} role="img" data-testid="sankey-svg" />;
  }
  ```

- [ ] **Step 8: 改 HomePage + IndustryDetail 加 footer**

  替换 `/home/hq/projects/industry-value-flow/frontend/src/pages/HomePage.tsx`:

  ```tsx
  import { useEffect, useState } from "react";
  import { useNavigate } from "react-router-dom";
  import { apiClient } from "../api/client";
  import { Sankey } from "../components/Sankey";
  import { Nav } from "../components/Nav";
  import type { SankeyData } from "../types/api";

  export function HomePage() {
    const [data, setData] = useState<SankeyData | null>(null);
    const [error, setError] = useState<string | null>(null);
    const navigate = useNavigate();

    useEffect(() => {
      apiClient.getIndustries().then(setData).catch((e) => setError(String(e)));
    }, []);

    if (error) return <div style={{ color: "red", padding: 24 }}>{error}</div>;
    if (!data) return <div style={{ padding: 24 }}>加载中...</div>;
    return (
      <div>
        <Nav />
        <div style={{ padding: 24 }}>
          <h1>行业价值流转</h1>
          <Sankey data={data} onIndustryClick={(id) => navigate(`/industry/${id}`)} />
          <footer data-testid="data-footer" style={{ marginTop: 24, fontSize: 12, color: "#666" }}>
            数据来源:{data.source ?? "占位数据"} · 年度:{data.year ?? "—"} · 单位:{data.unit}
            {data.source_url && (
              <>
                {" · "}
                <a href={data.source_url} target="_blank" rel="noopener noreferrer">
                  查看原始数据 →
                </a>
              </>
            )}
          </footer>
        </div>
      </div>
    );
  }
  ```

  替换 `/home/hq/projects/industry-value-flow/frontend/src/pages/IndustryDetail.tsx`:

  ```tsx
  import { useEffect, useState } from "react";
  import { useParams } from "react-router-dom";
  import { apiClient } from "../api/client";
  import { MiniSankey } from "../components/MiniSankey";
  import { PainPanel } from "../components/PainPanel";
  import { Nav } from "../components/Nav";
  import type { SankeyData } from "../types/api";

  export function IndustryDetail() {
    const { id } = useParams<{ id: string }>();
    const [data, setData] = useState<SankeyData | null>(null);

    useEffect(() => {
      if (id) apiClient.getIndustry(id).then(setData);
    }, [id]);

    if (!data || !id) return <div style={{ padding: 24 }}>加载中...</div>;
    return (
      <div>
        <Nav />
        <div style={{ padding: 24, display: "grid", gridTemplateColumns: "2fr 1fr", gap: 24 }}>
          <div>
            <h1>{data.industries[0].name}</h1>
            <MiniSankey data={data} />
            <footer data-testid="industry-footer" style={{ marginTop: 16, fontSize: 12, color: "#666" }}>
              数据来源:{data.source ?? "占位数据"} · 年度:{data.year ?? "—"} · 单位:{data.unit}
              {data.source_url && (
                <>
                  {" · "}
                  <a href={data.source_url} target="_blank" rel="noopener noreferrer">
                    查看原始数据 →
                  </a>
                </>
              )}
            </footer>
          </div>
          <div>
            <PainPanel industryId={id} />
          </div>
        </div>
      </div>
    );
  }
  ```

- [ ] **Step 9: 扩 api_client 测试**

  追加到 `/home/hq/projects/industry-value-flow/frontend/src/__tests__/test_api_client.test.ts`:

  ```ts
  it("getIndustries includes source/year/unit/source_url fields", async () => {
    const mock = vi.fn().mockResolvedValue({
      ok: true,
      json: async () => ({
        industries: [], nodes: [], edges: [],
        source: "国家统计局 2024", source_url: "https://x",
        year: 2024, unit: "亿元",
      }),
    });
    global.fetch = mock;
    const data = await apiClient.getIndustries();
    expect(data.year).toBe(2024);
    expect(data.unit).toBe("亿元");
    expect(data.source).toContain("国家统计局");
  });
  ```

- [ ] **Step 10: 跑所有前端测试**

  命令:
  ```bash
  cd /home/hq/projects/industry-value-flow/frontend && npx vitest run
  ```

  预期输出:全部 passed

- [ ] **Step 11: 类型检查**

  命令:
  ```bash
  cd /home/hq/projects/industry-value-flow/frontend && npx tsc --noEmit
  ```

  预期输出:`(exit 0)`

- [ ] **Step 12: 加 E2E-004/005/006**

  追加到 `/home/hq/projects/industry-value-flow/frontend/tests/e2e/industry-value-flow.spec.ts`:

  ```ts
  test("E2E-004: home 5 industries render with 2024 real values", async ({ page }) => {
    await page.goto("/", { waitUntil: "networkidle" });
    await expect(page.getByRole("heading", { name: "行业价值流转" })).toBeVisible({ timeout: 15_000 });
    // footer 含数据来源 + 年度 + 单位
    const footer = page.getByTestId("data-footer");
    await expect(footer).toBeVisible({ timeout: 10_000 });
    await expect(footer).toContainText("国家统计局");
    await expect(footer).toContainText("2024");
    await expect(footer).toContainText("亿元");
  });

  test("E2E-005: finance detail page shows 资金来源/机构/运用 3 columns", async ({ page }) => {
    await page.goto("/industry/finance", { waitUntil: "networkidle" });
    await expect(page.getByRole("heading", { name: "金融业" })).toBeVisible({ timeout: 10_000 });
    const footer = page.getByTestId("industry-footer");
    await expect(footer).toContainText("万亿元");
    await expect(footer).toContainText("央行");
  });

  test("E2E-006: finance page still renders even when scraper fails", async ({ page, request }) => {
    // 1. 先正常访问一次,让后端写入 cache
    const resp = await request.get("http://localhost:8000/api/industry/finance");
    expect(resp.ok()).toBeTruthy();
    // 2. 删 cache 模拟 scraper 失败,前端仍能渲染(用 fallback)
    // 这里仅断言 cache hit 路径能正确展示 9 节点
    await page.goto("/industry/finance", { waitUntil: "networkidle" });
    await expect(page.getByRole("heading", { name: "金融业" })).toBeVisible();
  });
  ```

- [ ] **Step 13: 跑 e2e(本地 backend + frontend 已起)**

  命令:
  ```bash
  cd /home/hq/projects/industry-value-flow/frontend && npx playwright test
  ```

  预期输出:`4 passed`(E2E-001 + E2E-004/005/006)

- [ ] **Step 14: 跑全量后端 + 前端 + e2e(模拟 CI)**

  命令:
  ```bash
  cd /home/hq/projects/industry-value-flow && source .venv/bin/activate && python -m pytest backend/tests/ && cd frontend && npx vitest run && npx tsc --noEmit
  ```

  预期输出:全部 exit 0

- [ ] **Step 15: 提交**

  ```bash
  cd /home/hq/projects/industry-value-flow && git add backend/domain/scraper/industry_association.py backend/tests/ frontend/src/components/Sankey.tsx frontend/src/pages/HomePage.tsx frontend/src/pages/IndustryDetail.tsx frontend/src/__tests__/ frontend/tests/e2e/industry-value-flow.spec.ts && git commit -m "feat: 金融业 3 层 9 节点 + 前端子色阶 + hover tooltip + footer source_url"
  ```

---

## 任务并行与 PR 策略

| Task | PR 标题 | 关联 | 合并顺序 |
|---|---|---|---|
| T1 | `feat(scraper): cn-stats + 主桑基图 2024 真实值` | — | 第 1 个 |
| T2 | `feat(schema): SankeyData +4 fields, edge ge=0, 启动清 v1 cache` | — | 第 2 个(可与 T1 并行,但合 main 必须等 T1) |
| T3 | `feat(scraper): 教育 8 学段真实数据` | T2 | 第 3 个 |
| T4 | `feat(scraper): 医疗 4 类机构真实数据` | T2 | 第 3 个(T3/T4 顺序无关) |
| T5 | `feat(scraper): 农业/制造业 11 大类真实数据` | T1 + T2 | 第 4 个 |
| T6 | `feat: 金融 9 节点 + 前端子色阶 + footer source_url` | T2 + T3/T4/T5 | 第 5 个 |

**合并策略**:
- T1 / T2 可同时建分支,但 PR 串行合 main(避免冲突)
- T3 / T4 在 T2 合 main 后**并行开发**
- T5 等 T1 + T2
- T6 等 T2 即可编码,等 T3/T4/T5 合 main 后再 PR(配色要看到完整子行业)

---

## Definition of Done(每个 Task 验收)

- [ ] 上述 step 全部 checked
- [ ] `pytest backend/tests/ -v` 全部 passed,覆盖率 ≥ 80%
- [ ] `cd frontend && npx vitest run` 全部 passed
- [ ] `cd frontend && npx tsc --noEmit` exit 0
- [ ] `cd frontend && npx playwright test` 全部 passed
- [ ] `git log --oneline -1` 显示本任务 commit
- [ ] PR 描述引用本计划对应 Task 编号(便于 reviewer 对照)

---

## 风险与回滚(spec §10 摘要,subagent 不需要重新决策)

| 风险 | 缓解 | 涉及 Task |
|---|---|---|
| R1 cn-stats 包失效 | fallback `industry_association.py` 真实硬编码 | T1, T5 |
| R3 0 值 edge | `ge=0.0` + 前端过滤 | T2, T6 |
| R4 网络抖动 | 3 次重试 + fallback 链 | T1 |
| R5 金融万亿元不一致 | UI footer 标注 | T2, T6 |
| R6 教育/医疗口径不同 | footer 各自标注 | T3, T4 |
| R7 cn-stats verify_ssl=False | warn log + 仅本地 | T1 |
| R8 节点 > 20 | 制造业 12 节点,在限内 | T5 |
| R9 字段 None 前端崩 | Pydantic 默认 + 前端条件渲染 | T2 |
| R10 旧 v1 cache | 启动时主动清 | T2 |

---

## 隐含约束(用户/架构平台规范,subagent 必读)

1. **CLAUDE.md 9 原则遵守**(本计划已遵守):高内聚低耦合(T6 前端配色按子色阶封装,无散落)、分层(scraper → schema → routes → tests)、复用(用现成 `cn-stats`、现成 `respx`)、资产(本批代码未来可作为 `cn-stats-adapter` 资产登记到 arch 平台)、反馈(若 cn-stats 失效,反馈 spec §10 R1)、质量 > 效率(覆盖率 ≥ 80%)、一致性(node id 命名约定 `agri_*` 等)、定位稳定性(scraper 只做抓取,不动 LLM 架构)。
2. **测试必须 mock 真实 HTTP**:所有 httpx 调用必须用 respx 拦截,cn-stats 同步包用 `monkeypatch` / `unittest.mock.patch` 替换(FB-647bc639 教训:漏掉 respx 会让单测通过但实际跑挂)。
3. **不要扩 scope**:本计划 6 任务边界严格,不做月/季度/历史/预测/i18n/移动端/部署/用户认证(原 spec §11)。
4. **commit 风格**:conventional commits,TDD 5 步 = 至少 5 commit/Task。
5. **arch 平台登记**:本批完成后,subagent 提交前调 `sdlc-registry-validate` 验证:`sankey-data-schema-v2` / `cn-stats-adapter` / `sub-industry-color-palette` 三个资产可登记。
6. **分支命名**:`feat/<req-id-prefix>-接入真实数据-T<n>`,例如 `feat/req-realdata-T1-cn-stats`。
7. **文档同步**:spec §11 列的"不做"清单,T6 不能因为要做金融而偷偷加时间序列。

---

**Plan 状态**: ✅ Ready for execution

**下一步**:用户批准后,调 `superpowers:subagent-driven-development` 按 T1 → T2 → (T3 ‖ T4) → T5 → T6 顺序执行