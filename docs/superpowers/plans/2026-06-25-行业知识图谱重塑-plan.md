# 行业知识图谱重塑 Implementation Plan (简短版)

> **基线**:基于已批准 spec `docs/superpowers/specs/2026-06-25-行业知识图谱重塑-design.md`
> **目标**:把"行业价值流转可视化平台"重塑为"行业知识图谱"

**Goal:** LLM 初始化生成 100+ 行业间相互支撑关系知识图谱,前端 react-flow 渲染,明色系企业风。

**Architecture:** 启动时 LLM 生成图 → 存 SQLite → react-flow force/dagre 布局 → 用户点击节点/边调 LLM 实时解释。

**Tech Stack:** FastAPI + Pydantic v2 + SQLite + Vite + React + TS + react-flow + dagre + 现有 LLM 抽象(5 provider)。

---

## 7 任务概览

| # | 任务 | 工作量 | 关键交付 | 依赖 |
|---|---|---:|---|---|
| **T1** | 废弃旧数据层 | 1.5d | 删除 `SankeyData` / `Sankey.tsx` / `cn-stats` / `industry_association.py` / `stats_gov.py` / `Footer.tsx` / 5 行业枚举 / 子色阶 | 无 |
| **T2** | 新数据模型 | 1.5d | `KnowledgeGraph` / `GraphNode` / `GraphEdge` / `Category`(GB/T 4754 大类 A-P)/ `RelationType`(provide/rely_on/service/consume)/ GB/T 4754 中类 100+ 代码清单 + 校验 | 无 |
| **T3** | GraphService | 1.5d | `graph_service.py` LLM 初始化 + **partial state 持久化**(每 node/edge 立即写 SQLite)+ 缓存(`graph:v1:{llm_config_hash}`, TTL 7d, partial 也缓存)+ `regenerate_failed()` 重跑接口 + LLM 不可用抛"请配置 LLM" | T2 |
| **T4** | API | 1d | `GET /api/graph`(含 stats:`{generated, failed, total}`)/ `GET /api/node/{id}` / `GET /api/edge/{id}` / `GET /api/edge/{id}/explain`(实时 LLM)/ **`POST /api/graph/regenerate-failed`**(重跑接口,scope=nodes|edges|all) | T2, T3 |
| **T5** | react-flow 集成 | 1.5d | `GraphView.tsx`(force/dagre 布局)+ 节点配色(GB/T 4754 20 大类,每类 1 色)+ 边样式(按 relation_type)+ 点击事件 | 无 |
| **T6** | 明色系企业风 UI | 1.5d | 全局 CSS tokens(白底 + 品牌蓝 #1A4D8F + 思源黑体)+ `HomePage`(react-flow 全图 + **顶部状态条:已生成 X / Y,失败 Z 个 [重跑]**)+ `NodePanel`(节点详情 + 入/出边)+ `EdgePanel`(边详情 + AI 解释按钮)+ **失败节点红色边框 + 失败边虚线** + tooltip 显示 failed_reason | T5 |
| **T7** | Fallback + 测试 | 1d | LLM 不可用白屏 + "请配置 LLM" 提示; 单测 + 集成测试 + **5 个 e2e**(react-flow 渲染 / 节点点击 / 边点击 / LLM 不可用 / **partial failure 重跑流程**) | T1-T6 |

**总工作量: 9.5 人天**(T3/T4/T6/T7 因 partial failure 机制增加工作量)

**并行轨道**:
- 轨道 A: T1(后端清理)+ T2(新数据模型)+ T5(前端 react-flow)— 无依赖,可并行
- 轨道 B: T3(GraphService) → T4(API)— 等 T2
- 轨道 C: T6(UI)— 等 T5
- 轨道 D: T7(收尾)— 等所有

---

## 关键全局约束(从 spec 直接 copy,所有任务隐含遵守)

- **节点**:GB/T 4754-2017 中类(100+ 项),LLM 生成
- **边**:定性关系(provide/rely_on/service/consume),weight 1-5,LLM 生成
- **可视化**:react-flow + dagre 布局(不持久化节点位置)
- **数据**:纯 LLM 驱动初始化,存 SQLite,cache key `graph:v1:{llm_config_hash}`,TTL 7 天
- **LLM 失败**:
  - LLM 完全不可用(无 key/网络挂):不缓存,启动报错;前端白屏 + "请配置 LLM" 提示
  - LLM **partial failure**(部分节点/边生成成功,部分失败):**每个 node/edge 立即持久化**(generated 或 failed),允许只重跑失败部分,不重跑成功部分
  - 失败节点 react-flow 用红色边框,失败边用虚线,hover tooltip 显示 failed_reason
  - 首页顶部状态条:`已生成 X / Y,失败 Z 个 [点此重跑失败部分]`
- **LLM 角色**:初始化 + 用户查询解释(中间不参与)
- **UI**:明色系企业风(白底 + 品牌蓝 #1A4D8F + 思源黑体 + 留白 24-32px)
- **持久资产**:LLM provider 抽象(5 provider)、Settings 页、SQLite 基础设施、pytest + Playwright
- **删除资产**:SankeyData / Sankey.tsx / cn-stats / industry_association.py / stats_gov.py / Footer.tsx / 5 行业枚举 / 子色阶 / E2E-004/005/006

---

## 保留 / 复用(直接拿来)

- `backend/domain/llm/` — 5 provider 抽象,**不动**
- `frontend/src/pages/Settings.tsx` — Settings 页,**不动**
- `backend/domain/storage/cache.py` — SQLite 缓存,接口复用(adapt 到 graph key)
- `backend/domain/rate_limiter.py` — 初始化时也要限速
- 测试基础设施:pytest + respx + vitest + Playwright
- CI:.github/workflows/ci.yml(不动)

---

## 风险与缓解

| 风险 | 缓解 |
|---|---|
| LLM 生成节点不覆盖所有 GB/T 4754 中类 | 不强制,生成的就行 |
| LLM 生成"幻觉关系" | validator 校验 source/target 必须存在 + relation_type 在枚举内 |
| LLM 启动失败(完全失败) | 不缓存,启动报错,前端提示"请配置 LLM" |
| LLM 部分生成成功 / 部分失败 | 每个 node/edge 立即持久化(generated/failed)+ 用户可重跑失败部分,7 天缓存保留 partial state |
| 100+ 节点 force 性能 | dagre 比 force stable,react-flow 100+ 节点性能足够 |
| 用户切 provider → 重新初始化 | llm_config_hash 变化触发 |
| LLM JSON 格式错误 | 严格 schema 校验 + 1 次重试 + 失败报错 |

---

## 不做(防止 scope creep)

- 节点编辑 UI(LLM 生成的图就是最终)
- 时间序列 / 历史快照
- 实时数据刷新
- 用户系统 / 权限
- 关系人工校验流程
- 导出图片 / PDF(留 backlog)
- 多进程 / 多用户并发(本地单人)

---

**Plan 状态**: ⏳ 等用户审
**用户审后**:进入 subagent-driven-development(派 7 subagent 并行执行 4 轨道)

**最近更新 (2026-06-25)**:加入 **partial failure 重跑机制**(用户决策)— GraphNode/GraphEdge 加 status 字段,持久化 partial state,`POST /api/graph/regenerate-failed` 只重跑 failed,前端状态条 + 失败节点红色边框 + 重跑按钮。详见 spec §4.4。